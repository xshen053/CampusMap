# Tests adding two nodes, one edge between two nodes
# and one points to itself

# Create a graph
CreateGraph TNOESD_graph

# Add two nodes
AddNode TNOESD_graph a
AddNode TNOESD_graph b

# Add one edge
AddEdge TNOESD_graph a a AA
AddEdge TNOESD_graph a b AB

# Print the nodes in the graph, the outgoing edges from a,
# and outgoing edges from b
ListNodes TNOESD_graph
ListChildren TNOESD_graph a
ListChildren TNOESD_graph b